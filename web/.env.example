# Public config is committed in `.env` and used by both local dev + CI.
#
# - Edit `web/.env` to change PUBLIC_* values
# - Copy `web/.env.local.example` -> `web/.env.local` for local-only secrets
#
# NOTE: `.env.local` is ignored by git and should never be committed.

